# Normalize line endings: LF in the repo for all text, checked out as-is.
* text=auto eol=lf

# Windows-native scripts keep CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binaries: never touch.
*.exe binary
*.png binary
*.jpg binary
*.ico binary
